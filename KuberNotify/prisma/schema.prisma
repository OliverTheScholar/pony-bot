generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Pods {
 

model Pods {
  id        Int     @id @default(autoincrement())
  name   String? @db.VarChar(50)
}
 id        Int     @id @default(autoincrement())
  nameP  tring? @db.VarChar(50)
}sSsS